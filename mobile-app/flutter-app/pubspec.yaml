name: smart_pothole_mobile
description: Smart Pothole Detection and Road Safety System mobile client
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.8.0+1
  flutter_map: ^6.2.1
  geolocator: ^13.0.2
  image_picker: ^1.1.2
  latlong2: ^0.9.1
  shared_preferences: ^2.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
